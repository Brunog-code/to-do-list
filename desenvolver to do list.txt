Passo a passo para desenvolver o to-do-list:

function gravar()

	-recuperar o array do localStorage (vem como JSON)
	-vrificar se tem item salvo
	-se tiver item salvo, converte JSON para array
	-cria o obj literal
	-verifica se tem algum input vazio
	-se tiver da msg e return
	-se nao, add item no array
	-exibir na tabela o que foi cadastrado chamando a function exibirNaTabela()
	-exibir msg pro usuario de item cadastrado com sucesso
	-zerar o input.